#include <Arduino.h>

// Motor pins (fixed by shield)
#define IN1 9  // Left motor direction (H bridge)
#define IN2 10  // Left motor direction
#define IN3 7   // Right motor direction
#define IN4 8  // Right motor direction
#define ENA 6   // Left motor speed (PWM)
#define ENB 5   // Right motor speed (PWM)